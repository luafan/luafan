version: 2
jobs:
   build-job:
     docker:
       - image: ubuntu:22.04
     steps:
       - run: apt update && apt install -y git
       - checkout
       - run: bash build_docker_ubuntu.sh || exit -1
       - run: echo "Build finished!"
workflows:
  version: 2
  build-deploy:
    jobs:
      - build-job